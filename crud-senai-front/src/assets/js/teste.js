const cores = Object.freeze({
    RED: "red",
    BLUE: "blue"
});

console.log(cores.RED);
console.log(cores.BLUE);

switch (cores) {
    case RED:
        
        break;
    case BLUE:
        
        break;

    default:
        break;
}